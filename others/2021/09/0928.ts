// function test(type: boolean): Array<any>
// function test(type: boolean): number

// function test<T extends boolean>(type: T) {
//   return type ? ([] as Array<any>) : (1 as number)
// }
// test(true)
